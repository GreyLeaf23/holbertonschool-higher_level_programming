-- Makes a table with a id and name description the name will be
-- received and it will not fail if the table already exists
CREATE TABLE IF NOT EXISTS `first_table` (`id` INT, `name` VARCHAR(256));
